# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/foodshare

# JWT Secret — change this to a strong random string in production
JWT_SECRET=foodshare_super_secret_key_2025

# Server port
PORT=5000

# JWT token expiry
JWT_EXPIRES_IN=7d
